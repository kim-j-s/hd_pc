<!DOCTYPE html>
<html lang="ko">
	<head>
	{{> layout/head}}
	<link rel="stylesheet" href="guide.css" />
	</head>
  <body class="guide_body{{#if gw_class}} {{gw_class}}{{/if}}">
    {{> @partial-block }}
  </body>
</html>
